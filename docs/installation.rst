Installation
============

Requirements
------------

Python 3.10 to 3.15 supported, including free-threaded builds.

Installation
------------

Install with **pip**:

.. code-block:: sh

    python -m pip install pslr

pslr ships binary wheels for common platforms, so installation does not normally require a Rust toolchain.
